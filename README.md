# python-intro-test
repository for the python intro course

this package can be used to generate descriptive statistics using csv files of datasets 


install with: py -m uv pip install -e .

run with: py -m uv run -m my_python_package
